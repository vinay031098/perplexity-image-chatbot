# perplexity-image-chatbot
End-to-end chatbot that uses Perplexity API as the backend for text queries and returns relevant images for the content entered. Built with Python backend and a clean frontend interface.
